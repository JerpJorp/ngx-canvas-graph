import { OverrideParameters } from './override-parameters';

describe('OverrideParameters', () => {
  it('should create an instance', () => {
    expect(new OverrideParameters()).toBeTruthy();
  });
});
